@model IEnumerable<CarsStore.Models.ViewModels.Blog.ArticleVm>
  @{
      Layout = "~/Views/Shared/_Layout.cshtml"; 
  }
<h2>All Articles</h2>

@if (User.IsInRole("BlogAuthor"))
{
    @Html.ActionLink("Add", "Add",null, new {@class="btn btn-lg btn-primary"})
}

<section>
    @foreach (var vm in Model)
    {
        <article>
            <h3>@Html.DisplayFor(vms => vm.Title)</h3>
            <p>
                @Html.DisplayFor(vms => vm.Content)
            </p>    
            <br/>
            <hr/>
            <span class="author">@Html.DisplayFor(vms=>vm.Author.Name)</span>
            <span class="publish-date">@Html.DisplayFor(vms => vm.PublishDate)</span>
        </article>
    }
</section>